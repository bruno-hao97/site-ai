export const AUTH_SPLIT_IMAGE =
  'https://mkt.cdnpk.net/web-app/media/magnific-image.webp';

export const AUTH_SPLIT_TAB_KEYS = [
  'auth.split.tab.image',
  'auth.split.tab.print',
  'auth.split.tab.video',
  'auth.split.tab.audio',
  'auth.split.tab.3d',
] as const;

export const AUTH_SPLIT_ACTIVE_TAB_KEY = 'auth.split.tab.image';
